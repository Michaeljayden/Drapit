[build]
  command = "npm run build"
  publish = ".next"

[functions]
  # Webhook handler heeft extra tijd nodig voor bg-removal + compositing
  node_bundler = "nft"

[[functions]]
  path = "/api/webhook/replicate"
  timeout = 60

[[functions]]
  path = "/api/tryon/process"
  timeout = 60
